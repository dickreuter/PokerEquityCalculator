import wrap

def montecarlo(my_cards, cards_on_table, players):
     equity = wrap.montecarlo(my_cards, cards_on_table, players, 1000)

if __name__=="__main__"
	my_cards = {'3H', '3S'}
	cards_on_table = {'8S', '4S', 'QH', '8C', '4H'}
	players = 2
    montecarlo(my_cards, cards_on_table, players)